# ContractAI 智能合同自动化架构方案

## 一、核心问题

当前系统的主要瓶颈：
1. **格式丢失** — 自定义 DocxFormatParser 解析后变成纯 HTML，丢失图片、页眉页脚、编号样式、主题色等
2. **导出断裂** — 编辑器里改的内容无法保真导出回 .docx，只能存为 HTML .doc
3. **起草是生成新文档** — 无法基于用户模板填充，只能从预置模板从零生成

## 二、目标架构

```
用户上传模板.docx + 附件(采购清单等)
       │
       ▼
┌──────────────────────────────────────────┐
│  模板解析层 (JSZip + AI)                   │
│  ┌────────────┐  ┌─────────────────────┐ │
│  │ 结构提取    │  │ 占位符识别           │ │
│  │ sections   │  │ 【xxx】{xxx} 空白   │ │
│  │ paragraphs │  │ 必填/选填分类        │ │
│  │ tables     │  │ 字段类型推断         │ │
│  └────────────┘  └─────────────────────┘ │
└──────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  智能填充层 (AI + 规则)                     │
│  ┌──────────┐ ┌──────────┐ ┌───────────┐ │
│  │ 附件解析  │ │ 知识库匹配│ │ 历史复用   │ │
│  │ 采购清单  │ │ 规则库   │ │ 之前的合同  │ │
│  │ 供应商信息│ │ 行业惯例 │ │ 已有信息   │ │
│  └──────────┘ └──────────┘ └───────────┘ │
│          │                                 │
│          ▼                                 │
│  ┌────────────────────────────────────┐   │
│  │ 信息差分析 → 智能追问               │   │
│  │ 已知信息自动填充                    │   │
│  │ 缺失信息逐项追问                    │   │
│  │ AI参考建议（行业标准条款等）         │   │
│  └────────────────────────────────────┘   │
└──────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  文档渲染层 (docx-preview)                 │
│  浏览器端高保真渲染原始 .docx               │
│  保留图片/表格/页眉页脚/字体/样式            │
│  叠加交互层（点击字段编辑/AI建议采纳）       │
└──────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  导出层 (JSZip + XML操作)                   │
│  在原始 .docx ZIP 内替换占位符文本           │
│  保留所有格式、图片、样式                    │
│  生成格式完美保真的新 .docx                  │
└──────────────────────────────────────────┘
```

## 三、技术选型

### 3.1 文档渲染 — docx-preview（新增）
```bash
npm install docx-preview
```
- 浏览器端直接渲染 .docx，保留图片、表格、页眉页脚、字体
- 使用 JSZip 解析 ZIP → XML → HTML+CSS
- 不修改原始文件，纯展示层

### 3.2 模板解析 — JSZip（已有）+ AI
- 服务端用 JSZip 读取 .docx 内的 word/document.xml
- 用 XML 解析提取段落、表格结构
- 正则 + AI 识别占位符（【xxx】、{xxx}、下划线空白等）
- AI 推断每个字段的类型（甲方名称、金额、日期等）

### 3.3 智能填充 — 大模型 API（已有）
- 解析附件（采购清单等）→ 提取结构化数据
- 匹配模板字段和附件数据
- 生成追问清单（缺失字段）
- 提供AI参考建议

### 3.4 文档导出 — JSZip XML 替换（改进现有）
- 在原始 .docx ZIP 内定位占位符 XML 节点
- 替换 <w:t> 文本内容，保留所有 <w:rPr> 格式属性
- 处理跨 run 的占位符（一个占位符被分成多个 <w:r>）
- 输出新 .docx，格式 100% 保真

### 3.5 审查 — 在 docx-preview 渲染层叠加
- AI 审查结果作为标注叠加在文档视图上
- 侧栏显示风险点列表
- 点击风险点定位到文档中对应位置
- 修改建议在文档视图中高亮显示

## 四、用户流程

### 4.1 合同起草（模板驱动）
```
步骤1: 上传模板.docx
  → 系统解析模板结构，识别字段
  → docx-preview 高保真渲染预览

步骤2: 上传附件（采购清单等，可选）
  → AI 解析附件内容
  → 自动匹配并填充已知字段
  → 高亮已填充的字段

步骤3: AI 追问
  → 逐项追问缺失信息
  → 对话式交互（像现在的合同对话页面）
  → 用户回答后实时更新文档预览

步骤4: AI 参考建议
  → 对选填字段提供行业标准建议
  → 用户可以选择采纳或自定义

步骤5: 确认生成
  → 用户确认所有信息
  → 系统在原始 .docx 内替换占位符
  → 下载格式完美保真的新合同 .docx
```

### 4.2 合同审查（在原文档中）
```
步骤1: 上传待审合同.docx
  → docx-preview 高保真渲染
  → AI 同步审查

步骤2: 审查结果展示
  → 文档视图中高亮风险片段
  → 侧栏显示风险列表
  → 点击风险项跳转到文档位置

步骤3: 修改建议
  → 侧栏给出修改建议
  → 用户确认后在文档视图中标记修改
  → 可一键接受全部建议

步骤4: 导出
  → 下载修改后的 .docx（格式保真）
```

## 五、关键实现细节

### 5.1 占位符识别规则
```
【xxx】      — 中文方括号占位符（最常见）
{xxx}        — 花括号占位符
_______      — 下划线空白
（xxx）      — 圆括号待填
具体需要补充  — AI 语义识别
```

### 5.2 跨 run 占位符处理
Word 中一个【甲方名称】可能被拆成：
```xml
<w:r><w:t>【甲方</w:t></w:r>
<w:r><w:t>名称】</w:t></w:r>
```
需要合并相邻 run 的文本后再匹配占位符，替换时保持第一个 run 的格式。

### 5.3 表格内占位符
采购合同模板中的表格（如价格明细表）也需要支持：
- 识别表格中的占位符行
- 根据采购清单数据动态填充行
- 保持表格样式和边框

### 5.4 图片处理
docx-preview 支持渲染图片（从 word/media/ 提取 base64）。
导出时图片保持不变（ZIP 内的 word/media/ 文件不动）。

## 六、实施阶段

### Phase 1: 文档渲染升级（1-2天）
- [ ] 安装 docx-preview
- [ ] 新建模板预览组件，替换 OfficeEditor 的显示功能
- [ ] 保留 OfficeEditor 用于纯文本编辑场景
- [ ] 验证图片、表格、页眉页脚渲染

### Phase 2: 模板解析引擎（2-3天）
- [ ] JSZip 读取 .docx XML
- [ ] 提取段落、表格结构
- [ ] 正则识别占位符
- [ ] AI 辅助推断字段类型
- [ ] 生成模板字段清单 API

### Phase 3: 智能填充流程（2-3天）
- [ ] 附件解析（采购清单 Excel/Word/PDF → 结构化数据）
- [ ] 字段匹配算法（AI + 关键词）
- [ ] 智能追问对话流程
- [ ] AI 参考建议生成
- [ ] 实时预览更新

### Phase 4: 保真导出（1-2天）
- [ ] XML 内占位符定位（跨 run 合并）
- [ ] 文本替换（保留格式属性）
- [ ] 表格行动态填充
- [ ] 输出新 .docx 验证

### Phase 5: 审查集成（1-2天）
- [ ] docx-preview 叠加标注层
- [ ] 风险点文档内定位
- [ ] 修改建议交互
- [ ] 保真导出修改后合同

## 七、依赖清单

| 包名 | 用途 | 新增/已有 |
|------|------|----------|
| docx-preview | 浏览器端高保真渲染 | 新增 |
| jszip | 服务端读取 .docx ZIP | 已有 |
| @xmldom/xmldom | XML 解析 | 已有 |
| mammoth | 纯文本提取（降级方案）| 已有 |

不需要 LibreOffice、不需要 Pandoc、不需要商业库。全部基于开源 JS 库实现。
