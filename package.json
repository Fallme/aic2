{
  "name": "aic2",
  "version": "1.0.0",
  "private": true,
  "description": "AI Contract - 智能合同管理平台",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check knowledge.js && node --check contract-generate.js && node --check contract-review.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.9.10",
    "dotenv": "^17.4.2",
    "jszip": "^3.10.1",
    "mammoth": "^1.12.0",
    "pg": "^8.20.0"
  }
}
